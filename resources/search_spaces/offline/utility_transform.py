"""Offline-specific utility transforms.

Transforms map Bernoulli parameter bounds (θ) → utility bounds for COUP.

The shared hinge-error-rate transform is now inherited from
resources/search_spaces/utility_transform.py (scenario="default").

Add offline-specific transforms here if needed.
"""

# Offline inherits from default, no specific transforms needed currently
